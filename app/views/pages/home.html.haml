%h1 jenre.co

%p Wait till all the awesome is here!

= form_tag :id => 'song-picker' do
  %div
    = label_tag(:artist, "Artist: ")
    = text_field_tag(:artist, nil, :placeholder => "e.g., The Police")
  %div
    = label_tag(:song, "Song title: ")
    = text_field_tag(:artist, nil, :placeholder => "e.g., Roxanne")
  %div
    = submit_tag("Add to playlist")
